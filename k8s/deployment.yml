apiVersion: apps/v1
kind: Deployment
metadata:
  name: restaurant-frontend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: restaurant-frontend
  template:
    metadata:
      labels:
        app: restaurant-frontend
    spec:
      containers:
        - name: restaurant-frontend
          image: IMAGE_PLACEHOLDER
          ports:
            - containerPort: 80
